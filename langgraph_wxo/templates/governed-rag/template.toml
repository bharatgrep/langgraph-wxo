name = "governed-rag"
description = "Retrieval flow with an external-data connection and postgres checkpointer."
post_render = "Planned for a future release."

# NOTE: Placeholder manifest. The template source files are not shipped yet;
# `lgwxo new` currently offers `react-tools` and `minimal`.
[variables]
description = "A governed retrieval-augmented agent."
