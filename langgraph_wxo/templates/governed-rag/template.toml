name = "governed-rag"
description = "Retrieval flow with an external-data connection and postgres checkpointer (Phase 2)."
post_render = "Planned for Phase 2 — see PRODUCT_ROADMAP.md."

# NOTE: This is a Phase 2 placeholder manifest. The template source files are not
# shipped yet; `lgwxo new` only offers `react-tools` and `minimal` in the MVP.
[variables]
description = "A governed retrieval-augmented agent."
